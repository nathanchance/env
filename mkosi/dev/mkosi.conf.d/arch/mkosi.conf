[Match]
Distribution=arch

[Content]
Packages=
        # base
        base-devel
        openssh
        systemd-sysvcompat
        tzdata

        # b4
        git-filter-repo
        patatt

        # development
        diffutils
        lib32-glibc
        shfmt

        # email
        lei
        mutt

        # env
        fish
        tmuxp
        vim-spell-en

        # git
        github-cli
        perl-authen-sasl
        perl-mime-tools
        perl-net-smtp-ssl
        repo

        # kernel / tuxmake
        aarch64-linux-gnu-binutils
        libelf
        inetutils
        mkinitcpio
        ncurses
        pahole
        riscv64-linux-gnu-binutils
        rust-bindgen
        time
        uboot-tools

        # kup
        perl-config-simple

        # LLVM/clang + build-llvm.py
        clang
        libxml2-legacy
        lld
        llvm
        ninja
        perf

        # man pages
        man-db

        # nicer GNU utilities
        bat-extras
        diskus
        eza
        fd

        # package building
        devtools
        dpkg
        pacman-contrib
        rebuild-detector
        rpm-tools

        # python
        python
        # spdxcheck.py
        python-gitpython
        python-ply
        # uv
        uv

        # QEMU
        edk2-aarch64
        edk2-ovmf
        libevent
        libutempter
        qemu-emulators-full
        qemu-guest-agent
        qemu-img
        virtiofsd

        # system administration
        ipmitool
        reflector

        # website management
        hugo
        iproute2

[Output]
ImageId=dev-arch
