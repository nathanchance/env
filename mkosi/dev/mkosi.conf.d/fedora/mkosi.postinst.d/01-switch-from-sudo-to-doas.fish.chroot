#!/usr/bin/env fish

rm -f /etc/dnf/protected.d/sudo.conf

if command -q dnf5
    set dnf dnf5
else
    set dnf dnf
end

if $dnf list --installed sudo &>/dev/null
    $dnf remove -y sudo
    or return
end
